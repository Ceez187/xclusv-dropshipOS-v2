SUPABASE_URL=
SUPABASE_SERVICE_KEY=
ANTHROPIC_API_KEY=
RAPIDAPI_KEY=
RAPIDAPI_HOST=
ALLOWED_ORIGIN=http://localhost:5173
PORT=8080

# Signup + login email alerts (optional — see README "Email alerts")
# GMAIL_USER/GMAIL_APP_PASSWORD are used by both. WEBHOOK_SECRET is only
# needed for the new-signup webhook (login alerts use the normal user
# session token instead).
GMAIL_USER=
GMAIL_APP_PASSWORD=
WEBHOOK_SECRET=
